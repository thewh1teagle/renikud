# Alignment Check 001

Validate the first 1,000 rows of `dataset/train.jsonl` for:

- alignment characters matching the source text after dropping punctuation
- spaces encoded as `[" ", " "]`
- IPA chunk shapes consistent with the aligner's per-letter phonology
